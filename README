EPSar v1.1                                  EUMETSAT (c) 2012-2013

EPSar is a tool for wrapping and unwrapping EPS products containing NetCDF data records.

Author: leonid.butenko@eumetsat.int

INSTALLATION

> perl Makefile.PL [INSTALL_BASE=/path/to/install]
> make install

USAGE

epsar -x -c -v -t [-m <MPHR>] [-C <output dir>] <EPS_Product> [file1] ... [fileN]

-x           Unwrap EPS product. e.g:
                 epsar -x <EPS_PRODUCT>
-c           Create EPS product from a set of product files (NetCDF4/BUFR) and MPHR file. e.g:
                 epsar -c -m <MPHR> <netCDF4/BUFR files to wrap>
-t           Lists the NetCDF4/BUFR files contained inside the EPS product.
-C           Output directory. Default ./
-v           Verbose output (default silent)
-m           For -c option defines MPHR file containing the correct product model
--extract    same as -x
--create     same as -c
--verbose    same as -v
--outdir     same as -C
--list       same as -t
--mphr       same as -m
